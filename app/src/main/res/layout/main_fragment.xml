<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="ru.impression.ui_generator_example.fragment.MainFragmentViewModel" />

        <variable
            name="component"
            type="ru.impression.ui_generator_example.fragment.MainFragmentComponent" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatTextView
            isVisible="@{viewModel.welcomeTextIsVisible}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{viewModel.welcomeText}" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> viewModel.toggleVisibility()}"
            android:text="Toggle visibility" />

        <ru.impression.ui_generator_example.view.TextEditorComponent
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <ru.impression.ui_generator_example.view.AnimatedTextViewComponent
            animation="@={viewModel.textAnimation}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            app:animatedText="Animated text" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> viewModel.animate()}"
            android:text="Animate" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:text="Current time is:" />

        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@{viewModel.currentTime}"
                tools:text="12345" />

            <ProgressBar
                isVisible="@{viewModel.currentTimeIsLoading}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center" />

        </FrameLayout>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> viewModel.reloadCurrentTime()}"
            android:text="reload" />

    </LinearLayout>

</layout>